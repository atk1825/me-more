<% if user_signed_in? && post.liked_by?(current_user) %>
  <%= link_to "保存済み", post_like_path(post.id), data: { turbo_method: :delete }, class: "post_likes" %>
<% else %>
  <%= link_to "保存する", post_likes_path(post.id), data: { turbo_method: :post }, class: "post_likes"%>
<% end %>